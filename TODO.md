# To do

- Allow Variable 3 to set the color of a scatter plot.
- Check plot's linear regression function - clearly not correct, as it does not match the multiple regression results and is not on the points when X and Y are the same variable
